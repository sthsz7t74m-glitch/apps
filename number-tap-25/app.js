const grid=document.querySelector('#grid');const timeEl=document.querySelector('#time');const nextEl=document.querySelector('#next');const bestEl=document.querySelector('#best');const progressEl=document.querySelector('#progress');const messageEl=document.querySelector('#message');const startButton=document.querySelector('#start');const resetBestButton=document.querySelector('#resetBest');const sizeDown=document.querySelector('#sizeDown');const sizeUp=document.querySelector('#sizeUp');const sizeLabel=document.querySelector('#sizeLabel');const totalLabel=document.querySelector('#totalLabel');let gridSize=5;let total=25;let expected=1;let playing=false;let startedAt=0;let animationId=0;const key=()=>`numberTapBest-${gridSize}`;function shuffle(values){for(let i=values.length-1;i>0;i-=1){const j=Math.floor(Math.random()*(i+1));[values[i],values[j]]=[values[j],values[i]]}return values}function getBest(){const value=Number(localStorage.getItem(key()));return Number.isFinite(value)&&value>0?value:null}function showBest(){const best=getBest();bestEl.textContent=best?best.toFixed(2):'--'}function updateSizeUi(){total=gridSize*gridSize;sizeLabel.textContent=`${gridSize}×${gridSize}`;totalLabel.textContent=`1〜${total}`;progressEl.textContent=`0 / ${total}`;nextEl.textContent='1';grid.style.setProperty('--grid-size',gridSize);grid.style.setProperty('--grid-gap',`${Math.max(2,8-gridSize*.65)}px`);grid.style.setProperty('--number-size',`${Math.max(11,29-gridSize*1.7)}px`);sizeDown.disabled=gridSize<=3;sizeUp.disabled=gridSize>=10;showBest();renderGrid(false)}function renderGrid(enabled=false){grid.replaceChildren();const numbers=shuffle(Array.from({length:total},(_,index)=>index+1));numbers.forEach(number=>{const button=document.createElement('button');button.className='number-button';button.type='button';button.textContent=number;button.disabled=!enabled;button.setAttribute('aria-label',String(number));button.addEventListener('pointerdown',()=>handleTap(button,number));grid.append(button)})}function updateTimer(){if(!playing)return;timeEl.textContent=((performance.now()-startedAt)/1000).toFixed(2);animationId=requestAnimationFrame(updateTimer)}function handleTap(button,number){if(!playing)return;if(number!==expected){button.classList.remove('wrong');void button.offsetWidth;button.classList.add('wrong');navigator.vibrate?.(20);return}button.classList.add('done');button.disabled=true;progressEl.textContent=`${number} / ${total}`;if(number===total){finishGame();return}expected+=1;nextEl.textContent=expected;messageEl.textContent=`${expected} をタップ！`}function startGame(){cancelAnimationFrame(animationId);expected=1;playing=true;timeEl.textContent='0.00';nextEl.textContent='1';progressEl.textContent=`0 / ${total}`;messageEl.textContent='1 をタップ！';startButton.textContent='やり直す';sizeDown.disabled=true;sizeUp.disabled=true;renderGrid(true);startedAt=performance.now();updateTimer()}function finishGame(){playing=false;cancelAnimationFrame(animationId);const result=(performance.now()-startedAt)/1000;timeEl.textContent=result.toFixed(2);nextEl.textContent='✓';const oldBest=getBest();const isNewBest=!oldBest||result<oldBest;if(isNewBest)localStorage.setItem(key(),String(result));showBest();messageEl.textContent=isNewBest?`新記録！ ${result.toFixed(2)}秒`:`クリア！ ${result.toFixed(2)}秒`;startButton.textContent='もう一度';sizeDown.disabled=gridSize<=3;sizeUp.disabled=gridSize>=10;grid.querySelectorAll('button').forEach(button=>button.disabled=true);navigator.vibrate?.([40,40,80])}function changeSize(delta){if(playing)return;gridSize=Math.min(10,Math.max(3,gridSize+delta));timeEl.textContent='0.00';messageEl.textContent='サイズを選んでスタート！';updateSizeUi()}startButton.addEventListener('click',startGame);sizeDown.addEventListener('click',()=>changeSize(-1));sizeUp.addEventListener('click',()=>changeSize(1));resetBestButton.addEventListener('click',()=>{localStorage.removeItem(key());showBest();messageEl.textContent=`${gridSize}×${gridSize}のベストを削除しました`});updateSizeUi();if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'))}